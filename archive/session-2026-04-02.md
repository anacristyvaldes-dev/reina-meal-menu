# Reina — Session Notes
**Date:** April 2, 2026

---

## What we worked on

### 1. Mobile preview
- Opened the app in mobile viewport (375×812)
- Confirmed header, subtitle, filter bar, and single-column card grid all look good on phone

### 2. Subtitle text experiment
- Changed "¿Qué hay de comer?" → "Hora de comer"
- Reverted back to **¿Qué hay de comer?** — didn't feel right

### 3. Typography: removed bold
- **Filter buttons** (`font-weight: 700` → `400`) — Barlow Condensed
- **Card titles** (`font-weight: 700` → `400`) — Barlow Condensed

### 4. Tag alignment (in progress)
- Goal: pin tags to the bottom of every card so they don't float around based on title length
- Fix: `margin-top: auto` on `.card-tags` (card body is already a flex column)

---

## Current state of key files

| File | Notes |
|------|-------|
| `index.html` | Header: REINA + ¿Qué hay de comer? Filter bar with 10 buttons. |
| `style.css` | Card titles + filter buttons now regular weight (400). Tags pending bottom-align fix. |
| `app.js` | No changes this session. |

---

## Design decisions locked in (from earlier sessions)

- **Header:** `#E0D857` yellow-green, REINA in `#F85E39` Bebas Neue 89px
- **Subtitle:** Inter 12px, `#1B4332` dark green, 0.34em letter-spacing, uppercase
- **Cards:** Square corners, no shadow, `1px solid #E0D5C8` border, `#F5F0E8` card body
- **Tags:** `#F2D9D0` background, `#F85E39` text, pill shape
- **Footer:** `reina-stamp.png`, 200px, `mix-blend-mode: multiply`, no tagline
- **Data source:** Google Sheets published CSV, fetched live via `fetch()`
- **Images:** Local `/images/*.png`, slug-matched to meal names

---

## Next up
- Confirm tag bottom-alignment fix looks good on mobile
